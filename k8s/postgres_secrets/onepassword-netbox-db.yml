---
# Mirrors the unified NetBox 1Password item into db-postgres so the
# postgres-provision-job can read db-password without cross-namespace access.
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: netbox
  namespace: db-postgres
spec:
  itemPath: "vaults/HomeLab/items/NetBox"
