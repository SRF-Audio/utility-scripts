---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-primitives
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: coachlight-k3s-infra
  source:
    repoURL: https://github.com/SRF-Audio/utility-scripts
    targetRevision: main
    path: k8s/cluster_primitives
  destination:
    server: https://kubernetes.default.svc
    namespace: storage-cluster-primitives
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
