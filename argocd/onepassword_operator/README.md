# onepassword_operator ArgoCD Application

This directory contains the ArgoCD Application manifest for the 1Password Operator deployment.

## Files

- `onepassword_operator.yml` - ArgoCD Application manifest (generated by Ansible role `onepassword_operator_deploy`)

## Sync Wave

This application uses sync-wave `1` to ensure proper ordering during ArgoCD synchronization.

## Related Role

- Ansible Role: `ansible/roles/onepassword_operator_deploy`
