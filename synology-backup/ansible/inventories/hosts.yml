---
# synology-backup/ansible/inventories/hosts.yml
#
# This inventory is intentionally scoped to the Synology NAS only.
# The main monorepo inventory (ansible/inventories/hosts.yml) is the
# authoritative source for all other hosts.
all:
  children:
    synology_nas:
      hosts:
        srfaudio:
          lan_ip: 192.168.226.6
          ts_ip: 100.64.218.83
