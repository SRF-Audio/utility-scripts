---
# Reuses the same k8s/tailscale_operator/onepassword/ path from the homelab.
# The OnePasswordItem CRD pulls from the same HomeLab vault item.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tailscale-operator-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: hetzner-infra
  source:
    repoURL: https://github.com/SRF-Audio/utility-scripts
    targetRevision: main
    path: k8s/tailscale_operator/onepassword
  destination:
    server: https://kubernetes.default.svc
    namespace: infra-tailscale-operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
