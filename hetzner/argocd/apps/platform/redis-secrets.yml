---
# Reuses k8s/redis_secrets/ verbatim — same HomeLab 1Password vault item.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: paperless-ngx-redis-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: hetzner-db
  source:
    repoURL: https://github.com/SRF-Audio/utility-scripts
    targetRevision: main
    path: k8s/redis_secrets
  destination:
    server: https://kubernetes.default.svc
    namespace: db-redis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
