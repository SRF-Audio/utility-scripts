# HOTAS Mappings — Claude Instructions

## What this project is
A static web app (nginx in K8s via ArgoCD on Hetzner) that renders controller mapping reference cards. Two visual modes:
- **VKB view** — overlay labels on a rendered PDF background image; fields positioned from extracted PDF form geometry (`assets/fields.json`)
- **Boopidoo view** — CSS/HTML diagram of the Boopidoo layout (no background image needed)

The HTML viewer is embedded in `configmap.yml` as a K8s ConfigMap. Mapping data lives in `tools/*.json`.

For player hardware specs and per-rig binding constraints, see `[[project-gaming]]` in memory.

## File layout
```
tools/
  _index.json                    # VKB profile list — update when adding VKB profiles
  _boopidoo_index.json           # Boopidoo profile list — update when adding Boopidoo profiles
  {vkb-profile}.json             # VKB per-game mappings (ed-flight, msfs2024, dcs-f16, etc.)
  boopidoo-{game}.json           # Boopidoo per-game mappings
  vkb_gladiator_{left,right}.csv # authoritative VKB button→action spreadsheet
  elite_dangerous_controls.csv   # ED control name reference
  prep.py                        # one-shot: processes VKB PDFs → assets/bg-*.png + assets/fields.json
assets/
  bg-lh.png / bg-rh.png   # VKB background images (generated, don't edit manually)
  fields.json             # VKB field positions (generated by prep.py)
configmap.yml            # K8s ConfigMap: index.html + all profile JSONs as separate data keys
deployment.yml / ingress.yml / service.yml / namespace.yml / kustomization.yml
```

## Supported sim profiles
### VKB (HOSAS — desktop VR rig)
| Profile ID | Sim | Status |
|---|---|---|
| `ed-flight` | Elite Dangerous — ship flight | Active |
| `ed-galmap` | Elite Dangerous — galaxy map | Active |
| `ed-onfoot` | Elite Dangerous — on foot | Active |
| `ed-srv` | Elite Dangerous — SRV | Active |
| `msfs2024` | MSFS 2024 | In progress |
| `dcs-f16` | DCS World — F-16C | Stub |

### Boopidoo (compact laptop HOTAS)
| Profile ID | Sim | Status |
|---|---|---|
| `boopidoo-ed` | Elite Dangerous | Active |
| *(future)* | MSFS 2024 | Not started |
| *(future)* | DCS World | Not started |

## VKB JSON schema
Each `tools/{profile}.json` has top-level `name`, `version`, and optional `notes`, plus `lh` and `rh` objects mapping VKB field IDs to display strings.

**Field ID convention:** `{cluster}_{direction}_{col}` where col `_1` = Joy_N integer, `_2` = action label. Axes use `AX_{name}`. Examples:
- `MT_A_2` — main trigger, primary action label
- `A3_B_1` — A3 hat down, Joy number
- `C1_E_2` — C1 cluster push, action label
- `D1_A_2` — D1 modifier (always `"MODIFIER"`)
- Layer-2 secondary bindings: append `(NN ACTION)` in caps to the `_2` value, e.g. `"Landing Gear (52 SHIP SPOTLIGHT)"`

The `vkb_gladiator_{left,right}.csv` files are the authoritative source for which Joy_N maps to which physical control.

## Boopidoo JSON schema
Each `tools/boopidoo-{game}.json` has: `name`, `subtitle`, `version`, `modes[]`, `top_left[]`, `top_right[]`, `push_left`, `push_right`, `left_stick`, `right_stick`, `keypad[]`, `pov{}`, `switch_note`, `contexts[]`, `notes[]`.

**Keypad entries:** `{ "cap", "joy", "type": "mode"|"common", "modes": [T,E,M,C strings], "fn", "cat" }` — use `""` for empty mode slots (renders as faded —).

**Mode object:** `{ "tag", "name", "legend", "joy", "pos", "style" }` — `style` must match a CSS var name (`travel`, `explo`, `mining`, `combat`).

**Color categories (`cat`):** `flight`, `fire`, `ui`, `panel`, `power` — map to `.c-{cat}` and `.b-{cat}` CSS classes.

## Conventions
- Prefer adding/editing JSON profile files over touching the HTML viewer unless the change requires a structural UI update
- When adding a new VKB profile: add entry to `tools/_index.json` AND create `tools/{id}.json`; both must be reflected in `configmap.yml` data keys
- When adding a new Boopidoo profile: add entry to `tools/_boopidoo_index.json` AND create `tools/boopidoo-{game}.json`; both must be reflected in `configmap.yml` data keys
- Deploy: ArgoCD watches this directory; commit + push triggers sync
